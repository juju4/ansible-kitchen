---

- hosts: all
  pre_tasks:
    - set_fact:
        kitchen_gemlist_version:
          - { name: rubyzip, version: 1.3.0 }
          - { name: test-kitchen, version: 2.0.1 }
        kitchen_gemlist: []
      when: >
        (ansible_distribution == 'Ubuntu' and ansible_distribution_major_version|int == 16)
  roles:
    - juju4.kitchen
